// Storage key for the application
export const STORAGE_KEY = 'dashfy'

// LocalStorage key for storing the selected mode (light/dark)
export const MODE_STORAGE_KEY = 'mode'

// LocalStorage key for storing the selected theme
export const THEME_STORAGE_KEY = 'theme'

// LocalStorage key for storing the selected wake lock
export const WAKE_LOCK_STORAGE_KEY = 'wake-lock'
