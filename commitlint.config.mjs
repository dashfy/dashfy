/**
 * Commitlint configuration for Dashfy monorepo
 * @see https://commitlint.js.org/reference/configuration.html
 */
const commitlintConfig = {
  extends: ['@commitlint/config-conventional'],
}

export default commitlintConfig
