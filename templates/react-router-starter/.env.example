# Environment variables for your Dashfy app.
# Copy this file to `.env` and fill in the values required by the
# extensions you add with `dashfy add <extension>`.

# Required by the `github` extension.
# Create a token at https://github.com/settings/tokens
# GITHUB_TOKEN=
